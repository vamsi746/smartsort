services:
  - type: web
    name: smartsort-backend
    env: python
    pythonVersion: 3.10.13   # ✅ FORCE this version!
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
